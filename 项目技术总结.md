# 基于导航信息的电池充电温度预调 —— 项目技术总结

> 联合汽车电子 USP 创新训练营项目
> 最终成果:10 万 case 双 Oracle 标签库 + 监督学习模型 + 三级安全层,
> test 集可行性 **100%**、平均得分差 **3.47/50**;分层搜索比同质量暴力搜索快 **17 倍**。

---

## 1. 赛题与问题建模

**任务**:给定导航路线(6 段:里程/限速/驱动功率/环境温度)与初始状态(T=0℃, SOC=65%),
决策预热**开启位置(等价于开启时刻)**,使充电总时长与能耗加权得分最大。

**物理模型**(一阶集总热模型,dt=0.5s 数值积分):

```
M·cp·dT/dt = I²R + η·P_heat − (h0 + kv·v)·(T − T_env)
```

- 放电电流低电流根:`I = 2P / (U_oc + √(U_oc² − 4·R·P))`
- 三张 MAP 双线性插值(U_oc / Rint / P_CHARGE,P_CHARGE 温度轴 23℃ 节点)
- 充电时间:终点状态查一次 P_CHARGE_MAP 恒功率(按官方 PDF 规范)

**约束(raw)**:`20.00 ≤ T_end ≤ 25.00℃,SOC_end ≥ 10.00%`

**评分**:30 分充电时间 + 20 分能耗,case 内可行集 min-max 归一化。

关键洞察:得分是**不可导黑盒**(C 仿真),最优解普遍贴约束边界(T_end ≈ 20.000℃),
且部分 case 可行窗极窄(最窄 0.5s 宽,全量 10 万中 254 个 ≤50 候选的窄窗 case)。

---

## 2. 技术路线总览

```
官方 SDK 模板 (student_solution.c)
        │  逐位抽取物理内核
        ▼
preheat_model.h (共享物理内核, 与提交代码/Oracle/ML 同源)
        │
        ├──▶ oracle_generator.c ──▶ oracle_raw/safe.csv (10 万 case 双 Oracle 标签)
        │        (多级搜索 + OpenMP + 断点续算)              │
        │                                                    ▼
        │                                            train_mlp.py (监督/模仿学习)
        │                                                    │ model_mlp.pt
        │                                                    ▼
        └──▶ physics.c ──▶ physics.dll ──▶ eval_offline.py (安全层 + 离线评测)
                     (ctypes 共享库)        ppo_finetune.py (PPO 直接优化得分)
```

核心设计原则:**一套物理内核,四处使用**(提交算法、Oracle 标签、ML 校验、RL 环境),
从根上消除"训练环境和评测环境不一致"的偏差。

---

## 3. 阶段 0:物理内核与 Oracle 生成器

### 3.1 物理内核抽取

- [oracle/preheat_model.h](oracle/preheat_model.h):从 student_solution.c **逐位(verbatim)**抽取,
  保证 Oracle 标签与提交算法的物理完全一致。
- [oracle/physics.c](oracle/physics.c):封装为 `physics.dll`(Windows)/ `libphysics.so`(Linux),
  Python 经 ctypes 调用,暴露三个接口:
  - `ph_dll_simulate`:单次完整仿真(朴素暴力的成本模型)
  - `ph_dll_simulate_many`:**一次前缀仿真 + 批量候选评估**(前缀缓存,实测加速 ×4.2)
  - `ph_route_totals`:路线总时长/总里程

### 3.2 Oracle 生成器([oracle/oracle_generator.c](oracle/oracle_generator.c))

多级搜索策略(L0→L3),配合"无加热前缀仿真 + 加热后缀"的候选评估结构:

| 级别 | 步长 | 作用 |
|---|---|---|
| L0 | 10s | 全局粗扫,保留**所有**可行区间 |
| L1 | 1s | 可行区间内精扫;L0 为空时全路线回退 |
| L2 | 0.1s | Top-10 候选 + 每个区间端点附近精修 |
| L3 | 0.01s | 仅审计(`--audit N`),复核 L2 最优解稳定性 |

工程特性:OpenMP 16 线程并行、2000-case 分块落盘、`--resume` 断点续算
(扫 CSV 末行 case_id,确定性接续)、不可行快速通道(constraint_penalty 引导,
连"刀锋 case"(全程加热仍差 0.0002℃)也能给出最优惩罚解)。

**门禁**:`--selftest` 复现官方 Mock 基准(342 可行 / 35.32km / 20.00℃ / 15.95% / 3.1017kWh / 947.19s),
每次重编译必须通过后才允许生成。

---

## 4. 阶段 1:数值精度锁定

dt 收敛测试(dt=1.0 → 0.5 → 0.25):|ΔT|max=0.0026℃,|Δt_start|max=0.2s,|Δscore|max=0.115
→ **锁定 dt=0.5**(精度与算力平衡点,300 case 基准 9.7s)。

---

## 5. 阶段 2:全量 10 万 case 标签生成与质量验证

### 5.1 生成结果

| 项目 | 数值 |
|---|---|
| 总量 | 100,000 case × 双 Oracle(raw + safe),无缺口 |
| 耗时 | 4.4 小时(0.158s/case,16 线程,含 40k 停-续一次) |
| 可行率 | raw 52.42%(52,420)/ safe 51.85%(51,854) |
| 可行 case 得分 | 均值 42.24 / 中位 46.41(Pareto 前沿上时间与能耗不可兼得) |
| 充电时间 | 均值 980s,范围 [780.5, 1081.7]s |
| 不可行 case | 47,580 个,其中 99.95% 为物理升温不足(T_end 最高 19.9998℃) |
| 窄可行窗 | 254 个 case(≤50 个可行候选),最窄 0.5s 宽 |

### 5.2 质量审计(三层证据)

1. **L3 审计**:200 个 case 以 0.01s 步长复核,L2 答案与 10 倍精细搜索差异 ≤0.09s(均值 0.04s)。
2. **队友标签交叉验证**(独立实现的另一套 Oracle):
   - 可行性判定一致率 99.957%(17/40,000 分歧,全部为贴边浮点/舍入效应)
   - tch_min MAE 0.02s,相关系数 1.000000 → **两套物理内核互相验证通过**
   - 1,844 个"队友可行/我方不可行"全部为 3 位小数距离舍入(T 违约 ≤0.0012℃)
   - 得分差 >1 分的 3.9% case 溯源为窄可行窗归一化病态(可行窗 0.0013s 时微秒差被放大)
3. **精度修复**:发现 CSV 4 位小数导致窄窗打分伪影(case 5173 曾复算出 50.85 分),
   min/max 列升级为 `%.6f`(6 位小数,窄窗下舍入误差仅折合 0.012 分),
   同时打分函数对时间/能耗项封顶 30/20(策略在可行集内数学上不可能超满分)。

---

## 6. 对照实验:我们的方法 vs 朴素暴力搜索

同机同测试床(100 case,含 30 个窄窗),同一物理内核:

| 方法 | 检出率 | 单case耗时 | 评估次数 | 与Oracle最优Δt | 外推10万case |
|---|---|---|---|---|---|
| 暴力 10s 步长 | 92.0%(窄窗 73.3%) | 0.12s | 255 | 4.0s | 3.2 h |
| 暴力 1s 步长 | 100% | 1.08s | 2,548 | 0.42s | 29.9 h |
| 暴力 0.1s 步长 | 100% | 10.8s | 25,479 | **0.000s** | **300 h** |
| **分层搜索(我们)** | 100% | **0.64s** | **1,067** | **0.000s** | **17.7 h** |

(Python 单线程口径;生产级 C 生成器双 Oracle + 16 线程实际 **4.4 h**)

量化结论:
1. **同质量快 17 倍**:与 0.1s 全扫逐位复现 Oracle 最优(Δt=0),评估次数少 24 倍;
2. **比"省一档"的暴力更快且更好**:0.64s < 1.08s,质量从差 2 分到精确最优;
3. **前缀缓存再叠 ×4.2**:最朴素的"每候选从头仿真"写法外推 10 万 case 需 52 天;
4. MLP 前向 **2.0µs/case**(10 万 case 共 0.2s),比任何搜索快 5 个数量级——ML 路线的部署价值。

---

## 7. 阶段 3:监督学习(模仿学习)

[ml/train_mlp.py](ml/train_mlp.py),六步规范:

1. **标签**:safe Oracle 最优开启时刻(留安全裕度),归一化为开启分数 t_start/t_total;
2. **特征**:30 维 = 24 维分段参数(6 段 × [s_km, v_kmh, P_kW, T_env])+ 2 维初始状态 + 4 维全局统计;
3. **网络**:MLP 骨干(128-128-64)+ 三头输出(主头开启分数 sigmoid + 辅助头 T_end/SOC_end,多任务学习);
4. **损失**:主头 Huber(β=0.02)+ 0.25 × 辅助头 SmoothL1;
5. **优化**:AdamW + 余弦退火 + 早停(patience 20);训练数据按 `feasible==1` 筛选(不可行 case 无有效标签);
6. **部署**:三级安全层兜底(见下节)。

结果(全量 10 万,80k/10k/10k 划分):

- 训练 200 epoch / 222s(CPU),**val MAE 49.50s**(部分数据 40k 版为 58.17s,数据翻倍提升 15%)
- test 集(90001-100000,5313 个可行 case)离线评测:

| 指标 | 数值 |
|---|---|
| 直通率(MLP 直接可行) | 69.41% |
| **安全层后总通过率** | **100.00%** |
| **平均得分差** | **3.47 / 50**(P95 17.55) |
| 开启时刻 MAE | 45.1s(P95 204.6s) |
| 吞吐 | 15 case/s(含物理校验) |

---

## 8. 在线安全层:三级瀑布 + 窄窗升级

[ml/preheat_env.py](ml/preheat_env.py) `safety_layer`:

```
direct    预测点直接可行(1 次仿真)
  ↓ 失败
local     ±30s @ 1s 局部搜索(61 次仿真)
  ↓ 失败
fallback  全路线 10s 粗扫 + 1s 细搜(~300+31 次仿真)
  ↓ 粗扫一无所获(可行窗 < 10s)
窄窗升级   惩罚引导逐级细化:最小违约粗点(必贴可行窗边界)→ ±12s @ 1s → ±2.5s @ 0.1s
  ↓ 仍失败
none      物理上无可行解
```

**窄窗升级是复盘修复的成果**:首版评测出现 7 个 none case,解剖发现全是 0.5~7.2s 宽的
极窄可行窗(10s 网格插不进去)+ MLP 预测偏差 94~203s(超出局部搜索范围)双重叠加。
修复后 7/7 救回,且落点距 Oracle 最优仅 0.0~0.6s,总通过率 99.87% → **100.00%**。

模式分布(5313 test case):direct 3688 / local 1121 / fallback 504 / none 0。

---

## 9. 阶段 4:PPO 直接优化得分

[ml/ppo_finetune.py](ml/ppo_finetune.py):单步上下文决策(contextual bandit),
策略 = MLP 骨干 + 高斯头(μ, σ=0.05 起步),奖励 = 可行时 `score − 25`(居中),
不可行时 `−(2·tv² + 20·sv²) − 25`(二次惩罚)。数据同样按 `feasible==1` 筛选。
300 iter / 863s / CPU。

| 指标 | MLP(监督) | PPO(强化) |
|---|---|---|
| 直通率 | 69.41% | **94.90%** |
| 总通过率 | 100% | 100% |
| 得分差均值 | **3.47** | 9.56 |
| 开启时刻 MAE | 45.1s | 125.4s |
| 平均仿真次数 | 49.1 | **11.5** |

**结论**:PPO 兑现了"梯度直接瞄准得分"的可行性承诺(直通率 +25.5 个百分点,
在线仿真量降到 1/4),但**得分精度不及监督学习**——高斯策略优化的是带 σ≈0.048
噪声的期望奖励,贴边最优解在噪声下期望奖励不如窗口中央,策略被拉向保守解
(MAE 125s 印证)。**最终部署方案:MLP + 安全层(得分最优),PPO 作为
RL 直接优化黑盒得分的实证与备选**(改进方向:σ 退火至 0.005 量级消除期望偏差)。

---

## 10. 工程过程与踩坑记录

| 问题 | 现象 | 根因与修复 |
|---|---|---|
| bat 脚本 LF 行尾 | "系统找不到指定的批处理标签 - EOF" | cmd.exe 解析 LF-only 多行 if 块错乱;转 CRLF |
| PS 脚本参数 `$Input` | `unknown arg: output/oracle` | `$Input` 是 PowerShell 自动变量,绑定成空值致 exe 参数错位;改名 `$InputPath` |
| CPU 满载争抢 | 单线程任务慢 2000 倍 | 16 线程 Oracle 生成期间训练/评测被压垮;错峰执行或限线程(`OMP_NUM_THREADS=4`) |
| 分块落盘测速误判 | 短窗口观察进度为 0 | 2000-case 分块才落盘,观察窗口需 > 分块周期 |
| CSV 精度伪影 | 复算得分 50.85 > 满分 | 4 位小数舍入 + 窄窗归一化放大;min/max 升 6 位小数 + 打分封顶 |
| 评测口径 bug | 基准实验 Δt 均值 85s | 选点规则(最小充电时间)与 Oracle(得分最大化)在 Pareto 前沿上不一致;统一为得分最大化口径 |
| 7 个 none case | 安全层回退失败 | <10s 窄窗 10s 网格全落窗外;惩罚引导逐级细化修复 |

其他工程决策:
- **SDK 目录合并**:旧 sdk(防夹成果 + 训练数据)与新 sdk1/sdk 合并为单一 `sdk/`,
  全部路径引用同步更新,三重终验(Oracle 门禁 / Python 管线 / 学生模板编译运行逐位一致);
- **服务器迁移适配**(未启用):平台感知库名、CUDA 设备支持、build.sh、requirements.txt;
- **断点续算**:40k 处停止 → 修复精度 → 无损续跑至 10 万,数据零浪费。

---

## 11. 交付物清单

```
USP/
├── sdk/                          # 唯一 SDK 目录(合并后)
│   ├── student_template/         # 提交算法:完整预热算法(与 physics.dll 交叉验证逐位一致)
│   ├── antipinch/                # 防夹考题成果(269 行实现 + runner + 结果图)
│   └── data/nav_train_100000.txt # 官方 10 万训练数据
├── oracle/
│   ├── preheat_model.h           # 共享物理内核(与提交代码逐位一致)
│   ├── physics.c/.dll            # Python ctypes 物理库
│   ├── oracle_generator.c/.exe   # Oracle 生成器(多级搜索+断点+审计)
│   ├── run_oracle.ps1            # 复现流水线(selftest/dtstudy/generate/resume)
│   └── output/                   # oracle_raw/safe.csv (100k) + top10 + audit
├── ml/
│   ├── preheat_env.py            # 物理包装 + 评分 + 三级安全层(含窄窗升级)
│   ├── train_mlp.py              # 监督学习 → model_mlp.pt (val MAE 49.50s)
│   ├── ppo_finetune.py           # PPO → model_ppo.pt (直通率 94.90%)
│   ├── eval_offline.py           # 离线评测(支持 MLP/PPO 对比)
│   └── bench_vs_bruteforce.py    # 分层搜索 vs 暴力搜索对照实验
└── prototype/                    # 前期原型(dt 收敛/鲁棒性/算法设计文档)
```

## 12. 核心数字速查

| 项 | 数值 |
|---|---|
| Oracle 标签 | 100,000 case × 2(raw/safe),4.4h,可行率 52.42% |
| Oracle 质量 | L3 审计偏差 ≤0.09s;与队友独立实现一致率 99.957% |
| 搜索效率 | 同质量(Δt=0)下比 0.1s 暴力快 17 倍;前缀缓存 ×4.2 |
| MLP | val MAE 49.50s;前向 2.0µs/case |
| 最终评测 | 通过率 100.00%,得分差 3.47/50(P95 17.55),MAE 45.1s |
| PPO | 直通率 94.90%(MLP 69.41%),得分差 9.56( σ 期望偏差待改进) |
