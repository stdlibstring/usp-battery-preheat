# 答辩问答手册 —— 电池预热 MLP 方案

> 使用方法：详解版（0.5 节）按评委三连问顺序可整段背诵；
> 后续章节是短版口径——每题先背【口径】（一句话结论，评委追问时的底线回答），再展开。
> 数字全部可溯源：`ml/eval_direct.py`（直出）、`ml/eval_window_search.py`（窗口）、
> `ml/train_mlp.py` / `ml/ppo_finetune.py`（训练配置）、C 端 A/B 计数器（`-DNO_MLP` 开关）。

---

## 0. 一分钟开场陈述（背稿）

> "我们的方案是一套**神经网络先验 + 物理搜索**的混合架构。
> 核心发现来自两次碰壁：先尝试用 MLP 直接输出最优预热时刻，
> 发现 1/3 的 case 连硬约束都不满足——因为物理仿真能分辨 0.2 秒的差异，
> 而神经网络只能分辨约 50 秒，差两个数量级，这条路原理上走不通；
> 再用 PPO 精调，直通率上去了，精度反而劣化一倍——因为最优解由物理唯一决定，
> RL 没有发挥空间。
> 转折在于重新提问：MLP 的优势从来不是精度而是速度——单次推理 3.8 微秒，
> 比一次物理仿真快三个数量级。于是我们让它做擅长的事：
> **MLP 圈定 ±120 秒的搜索窗口，物理仿真在窗口内精确收割，失败时全程扫描兜底**。
> 结果：仿真量从 2,553 次降到 298 次，**8.6 倍加速**，得分只比理论最优低 1.7/50，
> 且 100% 满足硬约束。我们还用 A/B 实验划定了这套方法的能力边界——
> 它加速的是笨重的系统性扫描，对已经高度剪枝的引擎收益趋近于零。"

---

## 0.5 评委三组核心问题·详解版（对答如流篇）

> 本节按评委最可能的三连问顺序展开，每题都是可直接说出口的完整回答。
> 短版口径见后续章节（Q 编号互参），这里是“讲透版”。

### A 组：MLP 本体（模型、数据、训练、效果）

#### A1 你的 MLP 有几层？怎么搭的？

> 输入 30 维，三个隐藏层 128/128/64（全部 ReLU），三个输出头，总参数 28,801。

具体说：输入 30 维 = 24 维路段特征（6 个路段 × 里程/平均车速/驱动功率/环境温度）
+ 6 个全局量（初始电池温度、初始 SOC、路线总时长、总里程、平均环温、平均车速）。
骨干净到可以手画：`30 → 128 → 128 → 64`，然后分三个头——

| 头 | 输出 | 激活 | 作用 |
|---|---|---|---|
| 主头 head_t | 1 维 | sigmoid | 预热开启时刻 / 路线总时长 ∈ (0,1) |
| 辅头 head_T | 1 维 | 无（回归） | 到达温度 T_end（辅助监督） |
| 辅头 head_soc | 1 维 | 无（回归） | 到达 SOC（辅助监督） |

为什么刻意做小？因为部署目标是车端 C 环境：无 Python、无推理框架，
我们全部手写前向（矩阵乘 + ReLU + sigmoid），模型越小越可控。
28,801 个参数导出为 float32 常量数组仅 113KB。

#### A2 怎么训练的？

> 纯监督学习（也可以叫模仿学习：模仿的是 Oracle 最优解）。

流程：官方 10 万条工况 → 离线物理搜索生成最优标签（Oracle）→
构建 30 维特征 → 标准化 → 训练。
优化器 AdamW（lr=1e-3，weight_decay=1e-4），batch 1024，
余弦退火学习率；上限 200 轮、patience=20 早停；
**每轮在验证集上算“秒级 MAE”，回滚保存最优 checkpoint**——
不赌最后一轮，防止收敛后震荡期的偶发好点被遗忘。
CPU 上几分钟训完，无任何训练技巧黑盒。

#### A3 用了多少数据？

> 10 万条官方工况，其中可行（有最优解）的 5.2 万条参与训练。

按 case_id 严格三分，零重叠：

| 划分 | case_id 范围 | 总数 | 可行（实际使用） |
|---|---|---|---|
| 训练 | ≤ 80,000 | 80,000 | 41,832 |
| 验证 | 80k ~ 90k | 10,000 | 5,275 |
| 测试 | > 90,000 | 10,000 | 5,313 |

#### A4 你是怎么用工况那个 txt 的？

> 逐条解析为 6 路段 × 4 属性的矩阵，拼上 6 个全局量作为输入。

`nav_train_100000.txt` 每条工况就是一条冬季行程：固定总里程 39 km，
由 6 个路段组成，每个路段 4 个属性——路段里程(km)、平均车速(km/h)、
驱动功率(kW)、环境温度(℃)。字节级解析后得到 (100000, 6, 4) 数组。
特征构造分两层：路段原始 24 维直接展开，再加 6 个派生全局量
（初始温度 0℃、初始 SOC 65%、总时长、总里程、平均环温、平均车速），
共 30 维。也就是说**模型看到的是与物理仿真完全相同的输入信息**，
没有信息差，性能上限只受任务本身限制。

#### A5 对于无解的情况也训了吗？

> 没训，也不需要训。

10 万条里 47.6% 的 case 无可行解（怎么预热都到不了 [20,25]℃ 或 SOC 不够）。
这些 case 不存在 `best_start_time` 标签，监督学习无目标可学。
更重要的是职责划分：**判定“无解”是物理搜索的职责，不是神经网络的职责**——
线上方案先跑物理可行界定位（这部分不依赖 MLP），无可行域直接按赛题要求
输出不可行处理；MLP 只在“确认有解”的 case 上提供先验。
若评委追问“能不能让网络学会预测无解”：可以加一个分类头，
但物理可行界定位在线上本来就要做（用于剪枝），再加一个网络预测是重复劳动。

#### A6 损失是怎么设计的？

> 主头 SmoothL1 + 双辅头多任务加权。

总损失 = `SmoothL1(pred_frac, label_frac; β=0.02)`
+ 0.25 × SmoothL1(pred_T_end, label_T_end; β=0.1)
+ 0.25 × SmoothL1(pred_SOC_end, label_SOC_end; β=0.1)。

设计意图三点：(1) 主头用小 β 的 SmoothL1——小误差区接近 L1，
对个别离群标签更稳健（Oracle 标签在窄窗 case 上可能有网格量化误差）；
(2) 目标是“时刻/总时长”分数而非秒数，天然归一化、跨路线共享尺度；
(3) 两个物理量辅头是辅助监督——强迫骨干网络同时学“这个决策会导致什么物理后果”，
学到的表征更物理一致，间接提升了主头精度。辅头不参与线上决策。

#### A7 训了多少轮收敛？

> 上限 200 轮 + patience 20 早停，实际几十轮就到验证集最优。

之后主要是震荡（学习率余弦退火下也不再大幅下降），早停触发后回滚到
验证 MAE 最优的那一轮。我们不靠“训得久”，靠“选得准”。

#### A8 模型的输出是什么？

> 一个 (0,1) 分数，乘以路线总时长就是预热开启时刻（秒）。

例如 sigmoid 输出 0.316，路线总时长 2,789s，则预测在出发后 881s 开启预热。
这个设计的额外好处：**永远不可能预测出路线外的时间**，物理上自洽。
另两个输出（T_end/SOC_end）只在训练时做辅助监督，线上不用。

#### A9 模型的效果怎么样？

> 单看一个数字没意义，它在三个角色里效果完全不同——这恰恰是我们论证的核心。

| 评价角色 | 直通率 | 精度 | 结论 |
|---|---|---|---|
| 直接替代物理（直出） | 67.8% | MAE 52.2s，全体口径得分差 17.15 | ❌ 不可用：1/3 违反硬约束直接 0 分 |
| 加安全层（吸附到可行域） | 100% | 得分差 3.47 | ⚠️ 可用但平庸 |
| 圈窗口 + 物理精搜 | — | 得分差 1.74，仿真量 -88.3% | ✅ 甜点区 |

一句话总结：**它的精度不够格做决策者，但足够格做向导**——
预测误差 P95 约 200s，圈一个 ±120s 窗口绰绰有余。

#### A10 与什么对比的？baseline 是什么？

> 三重对比，全部同口径同数据。

(1) **主 baseline：全程物理扫描**（同一引擎关掉 MLP，`-DNO_MLP` 编译开关，
A/B 代码除 MLP 块外逐位相同）——这是回答“加 MLP 有什么用”的对照；
(2) **纯 MLP 直出 / MLP+PPO 直出**——这是回答“为什么不能全用神经网络”的对照；
(3) **两代物理引擎**（v1 全扫型 / v2 自适应精修型）——这是回答“MLP 的适用边界”。
全部对比在同一批 test case、同一物理引擎、同一评分公式下进行。

#### A11 用了多少工况测试？

> 窗口搜索评估 5,313 个 test 可行 case（与训练集零重叠）；
> 直出评估从中固定种子抽 2,000；C 端 A/B 用赛题 mock 路线。

test 段 case_id > 90,000，训练时从未见过。所有评估脚本
（`eval_direct.py` / `eval_window_search.py` / `eval_offline.py`）
固定随机种子，结果可复现。

### B 组：强化学习

#### B1 用了强化学习吗？怎么用的？

> 用了，尝试阶段的重要一支：PPO 微调 MLP 策略，最终没进提交版。

问题形态是单步上下文决策（contextual bandit 型 episode）：
状态 = 同样的 30 维特征；动作 = 预热开启时刻分数；
策略 = 对角高斯 N(mu(x), σ)，σ 可学习（初始 0.05）；
奖励 = **官方得分公式** - 25（居中），不可行时二次惩罚
`-(2×(ΔT² + 10×ΔSOC²)) - 25`，仿真崩溃 -50。

PPO 配置：从模仿学习 MLP **热启动**（共享骨干，只换输出头），
clip 0.2，每迭代 4 个 epoch，300 迭代 × batch 512，lr 3e-5，
熵正则 0.003，梯度裁剪 1.0；每 10 迭代在验证段评直通率+平均奖励，
存最优 checkpoint。

#### B2 为什么最终方案里没有 RL？

> 因为数据告诉我们它学错了目标：直通率上去了，得分下来了。

2000 case 直出对照（`eval_direct.py`）：

| | 直通率 | MAE | 可行子集得分差 | 全体得分差 |
|---|---|---|---|---|
| MLP 直出 | 67.8% | 52.2s | 4.53 | 17.15 |
| PPO 直出 | **94.95%** | **127.2s** | **9.89** | 11.08 |

直通率提升 27 个点，但预测误差翻倍、可行 case 上的选点质量劣化一倍多。
既然 RL 版本在它唯一该擅长的指标（得分）上更差，就没有理由进提交版。
但它不是白做：RL 实验里“不可行惩罚结构”的设计经验，沉淀进了
safe 标签的约束收紧与线上兜底逻辑——**以设计经验的形式保留**。

#### B3 为什么 RL 的直通率会上升？

> 因为奖励函数里“越界惩罚”是最强、最确定的梯度信号。

不可行惩罚是二次型（越界越多罚得越陡），可行域内得分平稳。
策略网络降低损失最快的方式，就是把均值 mu 从所有约束边界推开、
把方差 σ 收缩——也就是学会“**待在可行域中央，别犯错**”。
这解释了 67.8% → 94.95%：它确实学会了避免硬约束违反。

#### B4 那为什么得分反而下降？

> 因为这个任务的最优解恰恰贴在约束边界上，“别犯错”和“贴边做到最好”冲突。

三层机制：
(1) **最优解贴边**：最优开启时刻往往让到达温度恰好压在 20℃ 下限
（mock 路线最优就是 T_end=20.000℃），推向可行域中央 = 系统性偏离最优带；
(2) **RL 优化期望奖励**：在惩罚主导的 reward 下，“少犯错”的期望收益远大于
“贴边再好一点”，这不是调参问题，是目标函数结构的必然；
(3) **没有发挥空间**：给定工况，最优开启时刻由物理规律唯一决定，
策略网络没有可探索的自由度——探索只引入方差（MAE 翻倍就是证据）。
一句话：**本任务是“求解”不是“博弈”，RL 的强项（探索、权衡长期回报）
在这里没有兑现渠道**。

### C 组：MLP 的作用、加速与部署

#### C1 那你这个 MLP 到底起到了什么作用？

> 一句话：它是**先验/向导**——用 3.8µs 的推理替代掉大部分毫秒级物理仿真，
> 把“最优解在哪”的大致位置告诉搜索器，让物理仿真只在 ±120s 的小区间里收割。

分工原则来自误差预算：物理仿真分辨率 0.2s，MLP 误差约 50s，差两个数量级——
**精度必须归物理；但圈窗口只需要误差 < 120s，粗定位交给 MLP**。
它是优化器，不是保底者；有建议权，没有决策权。

#### C2 怎么加速的？

> 三步机制：窗口 → 剪枝 → 兜底。

(1) MLP 前向一次（3.8µs）得 t_pred，粗扫范围从**全程**（平均 2,553 个网格点）
收缩到 **t_pred±120s**（约 240 个点）；
(2) 窗口再与物理可行界求交集（可行界由不依赖 MLP 的二分定位给出，免费剪枝），
交集不足 30s 判定失效；
(3) 窗口内找不到可行点 → 自动回退全程扫描，精度与纯物理完全一致。
注意加速的本质：**省掉的是“确定最优不在哪”的排除成本**，
窗口内的精搜一步没省——这是零精度损失的根源。

#### C3 加速了多少？和什么对比的？

> 主口径 8.6×；另外两个口径一并报告，不挑最好看的说。

| 口径 | 对比基准 | 加速 | 精度 |
|---|---|---|---|
| 离线窗口搜索（5,313 case） | 同引擎全扫 2,553 次/case | 298 次 = **8.6×**（-88.3%） | 得分差 1.74（理论满分 50） |
| C 端 v1 引擎（mock） | 全扫 2,810 次 | 121 次 = **23×**（4.3%） | 与全扫逐位一致 |
| C 端 v2 自适应引擎（mock） | 990 次 | 980 次 = -1% | 逐位一致 |

第三个口径是诚实披露：v2 引擎的精修级联已是开销大头，MLP 压不掉它。
我们因此给出方法边界：**MLP 的收益 ∝ 引擎的“笨重程度”**。

#### C4 测了多少 case？

> 离线统计 5,313（全量 test 可行 case）；直出对比 2,000（固定种子抽样）；
> C 端 A/B 为赛题 mock 路线 + 9 类扰动场景鲁棒性测试。

全部与训练集零重叠，脚本固定种子可复现。

#### C5 你觉得这个东西可以部署到车上吗？

> 架构上可以，且我们的设计恰好是“功能安全友好”的；但要补三件事。

可行性论据：28,801 参数 / 113KB float32 常量 / 无运行时依赖
（纯 C 矩阵乘，无 Python 无框架）/ 单次前向 3.8µs——
这个量级对车规 MCU 完全无压力；权重编译进固件可做 CRC 校验，确定性行为。
最关键的是架构性质：**MLP 只影响速度不影响正确性**——
任何 ML 失效（分布漂移、传感器错、模型烂）最坏退化为纯物理基线，
这是功能安全评审最喜欢的失效模式。
要补的三件事：(1) 用真实车队数据重训（当前训练分布是官方合成工况）；
(2) 车端物理模型与仿真模型的一致性标定；(3) 按 ISO 26262 做完整论证。

#### C6 如果 MLP 输出的结果不好怎么办？

> 三道防线，逐层拦截，最坏情况等于基线——**永不更差**。

第一道：预测窗口与物理可行界求**交集**——MLP 错得离谱时交集不足 30s，
直接判定失效（物理可行界不依赖 MLP，错不到哪去）；
第二道：窗口内物理搜索**找不到可行点** → 自动回退全程扫描，
发生率 2.24%，回退 case 的精度与纯物理完全一致；
第三道：就算 MLP 彻底瞎了（比如换成随机数），
代价也只是回退率上升、速度变慢，**决策正确性不受任何影响**。
所以 MLP 在系统里从没有被信任到“输出即执行”的程度——
它的每个输出都要过物理这道关卡。

---

## 1. 定位与创新点

### Q1 你们的创新点到底是什么？
**口径**：不是"用了神经网络"，而是"用数据证明了神经网络在该任务中的正确位置"。

展开三层：
1. **架构**：MLP 先验缩区间 → 窗口物理精搜 → 全程兜底的三段式；
2. **证据链**：四方案同口径对比（纯 MLP / MLP+PPO / 纯物理 / 混合），
   混合方案 298 次仿真 + 1.74 分差，同时占据速度与质量甜点区；
3. **边界刻画**：两代引擎 A/B（-88.3% vs -1.0%），明确适用条件——加速"笨重扫描"而非"聪明搜索"。

### Q2 最终提交哪个版本？为什么？
**口径**：`student_solution_fast.c`（队友自适应引擎 + MLP 先验）。

展开：v2 引擎本身极快（自适应精修把粗扫压到总成本 1% 以下），MLP 在其上是
零精度损失的免费保险；若评测环境只给有限计算时间或路线更长，MLP 窗口的价值会放大。
**若追问"那 MLP 不就没用了吗"**：见 Q19。

### Q3 这和"调库跑个模型"有什么区别？
**口径**：模型只是链条一环，我们交付的是完整闭环——
Oracle 标签生成（分层搜索 17×）→ 训练 → C 端零依赖部署（28,801 参数手写前向）→
A/B 计数器量化收益，每一环都可复现。

---

## 2. 数据与 Oracle

### Q4 10 万条工况怎么来的？会不会过拟合这个分布？
**口径**：官方训练工况文件（`nav_train_100000.txt`，固定 39 km、初始 0℃/65%），
我们未改动分布；模型只用了 3.8 万参数、30 维特征，容量远小于数据量，结构性过拟合风险低。
**若追问"换分布就完了？"**：方案对分布漂移天然鲁棒——MLP 失准只导致窗口偏移，
回退全程扫描后精度与纯物理完全一致，损失的是速度不是正确性（回退率目前 2.24%）。

### Q5 Oracle 标签凭什么是最优？
**口径**：四层递进分层搜索 + 与暴力对拍验证。

展开：L0 10s 全局粗扫定可行域 → L1 1s 加密 → L2 0.1s 候选邻域 → L3 0.01s 审计；
与 25,479 次评估的同精度暴力搜索对拍，**最优开启时刻逐位一致（Δt=0）**，
而评估次数 1,067 次（17× 加速）——这是 10 万标签能在数小时内生成的前提。
**若追问"0.01s 审计有必要吗"**：最优解常贴约束边界，边界附近得分梯度陡，
粗网格会系统性漏掉贴边最优点（mock 路线最优恰为 T_end=20.000℃）。

### Q6 为什么要 safe / raw 双标签？
**口径**：safe 收紧约束（[20.05,24.95]℃/SOC≥10.05%）留 ~3.5s 安全裕度，
用 safe 训练、raw 评测，吸收模型与在线仿真的数值偏差。
实测代价：标签偏移 mean 3.5s，对应得分差仅 0.07——几乎免费的保险。

---

## 3. MLP 设计

### Q7 为什么是 MLP？不用 Transformer / GBDT？
**口径**：任务输入是固定 30 维表格特征、输出一维标量，MLP 足以拟合；
更关键的是**部署约束**：要在无运行时依赖的 C 环境手写前向，MLP 的矩阵乘最简单可控。
**若追问"30 维特征会不会太少"**：特征是完整物理输入（6 段×4 属性 + 6 个全局量），
信息无损——瓶颈不在特征而在任务本身的预测上界（见 Q9）。

### Q8 输出为什么用 sigmoid 分数而不是直接回归秒数？
**口径**：归一化到 [0,1]（除以路线总时长）使不同长度的路线共享同一输出尺度，
训练稳定且天然不会预测出路线外的时间。

### Q9 MAE 49.5 秒，误差不小吧？
**口径**：误差确实压不下去，但我们**不需要**它下去。

展开误差预算：52.2s（直出口径）= 3.5s safe 标签偏移（不可消除）+ 48.7s 模型误差
（受数据与容量限制，稳定在 40-50s）。关键对比：任务要求的分辨率是 0.2s（精搜网格），
MLP 差两个数量级——**所以精度必须交给物理**；但圈 ±120s 窗口只需误差 <120s，
MLP 绰绰有余——**所以粗定位交给 MLP**。同一个 50s，对"输出答案"是死刑，
对"圈窗口"是通行证。

### Q10 三个输出头是干什么的？
**口径**：主头预测开启时刻分数（sigmoid），两个辅头预测到达温度/SOC 做多任务辅助监督，
帮助 backbone 学到物理一致性，权重 0.25。辅头不参与线上决策。

---

## 4. PPO

### Q11 为什么试 PPO？为什么放弃？
**口径**：想让 RL 直接对齐比赛得分（reward=score），结果"直通率上升、得分反降"，
数据说明它学错了目标。

展开（2000 case 直出对比）：直通率 67.8%→**94.95%**，但 MAE 52.2→**127.2s**，
可行子集得分差 4.53→**9.89**。机制：PPO 为避免越界惩罚，把决策推向**可行域中央**——
学会了"别犯错"，牺牲了"贴边做到最好"；而本任务最优解恰恰贴边界。
**若追问"调 reward 能修吗"**：修得了直通率修不了根本——最优解由物理规律唯一决定，
策略网络没有可发挥的自由度，RL 的探索能力在此任务上没有兑现渠道，只会引入方差。

### Q12 那你们最终方案里 RL 的角色是什么？
**口径**：MLP 监督学习充当"先验"，PPO 的探索结论（不可行惩罚结构）沉淀进了
safe 标签与兜底设计——RL 以设计经验的形式保留，不在线上运行。

---

## 5. 混合架构（核心区，必考）

### Q13 为什么 MLP 不能替代物理搜索？（最重要的一题）
**口径**：分辨率差两个数量级，且有可行性风险。

三点：(1) 分辨率：物理 0.2s vs MLP ~50s，得分对开启时刻在最优附近是陡峭的，
50s 偏差对应数分损失；(2) 可行性：直出 32.2% 的 case 违反硬约束，直接 0 分；
(3) 硬约束边界形态：最优解常贴 T_end=20.000℃ 下限，回归型网络对"恰好压线"
这种条件极值天然弱势。

### Q14 8.6× 加速怎么算的？可信吗？
**口径**：同引擎同口径——全扫平均 2,553 次仿真/case，MLP 窗口后 298 次（11.7%），
比值 8.6。5,313 个 test case 统计，不是个例。
C 端独立验证：v1 实现在 mock 路线上 121 次仿真 = 全扫的 4.3%（更狠，
因为 mock 可行窗窄）。

### Q15 ±120s 窗口怎么定的？
**口径**：数据扫出来的 trade-off 最优点。

| 窗口 | 仿真次数 | 得分差 mean | P95 | P99 |
|---|---|---|---|---|
| ±60s | 296 | 1.78 | 6.45 | 14.56 |
| ±120s | 298 | 1.74 | **5.02** | **7.67** |

窗口宽一倍仿真量几乎不变（瓶颈是精搜不是粗扫），但尾部风险显著收窄——
所以选 ±120s。**若追问"更宽呢"**：宽到覆盖全程就退化成纯物理，
MLP 价值归零；±120s 是"预测误差 P95≈200s"与"搜索成本"的平衡点。

### Q16 1.74 分的损失从哪来？能消掉吗？
**口径**：来自窗口内**选点准则**的失真，不是窗口本身。

展开：窗口内用批间 min-max 归一化选点，但窗口局部的充电时间/能耗范围被压缩，
30:20 的有效权重比例畸变，会系统偏向窗口一侧（mock 实测：批选点 38.94 分
vs 窗内真实最优 41.25 分）。理论修正：得分排序等价于
`argmin(30·tch/R_C + 20·E/R_E)`，但 R_C（可行集充电时间极差）跨 case 分布极宽
（0.34s~38.78s），固定常数法实测 P95 反而变差——在线估计 R_C/R_E 是下一步工作。
**若追问"用真实范围能到多少"**：上界实验（W120-true）mean 0.46 分，
说明修复选点准则还有 ~1.3 分空间。

### Q17 窗口失效（预测离谱）怎么办？
**口径**：三级保险。交集 <30s 或窗口内无可行点 → 自动回退全程扫描，
发生率 2.24%，回退 case 的精度与纯物理完全一致——**最坏情况退化为基线，永不更差**。

---

## 6. 边界与诚实度（评委考察成熟度）

### Q18 方案什么时候会失效？
**口径**：三个已知边界，都有数据。

1. **窄窗 case**：safe 约束下双可行窄窗样本为 0，训练集天然缺失最难样本——
   但线上由全程兜底保护，损失速度不损失精度；
2. **聪明引擎**：对 v2 自适应精修引擎只省 1%（Q19 详述）；
3. **分布漂移**：MLP 失准仅降速（回退率升），不降正确性。

### Q19 (2)+MLP 只省 1%，这个融合还有意义吗？
**口径**：这次 A/B 的意义是**划定方法边界**，不是失败。

展开：v2 的自适应精修已把粗扫压到总成本 1% 以下，开销大头是 980 次精修级联仿真——
MLP 不敢碰它（动了掉分）。A/B 结果 980 vs 990、输出逐位一致，恰恰证明：
**MLP 的收益 ∝ 引擎的"笨重程度"**。给越原始的扫描配 MLP 收益越大（v1：-88.3%）。
知道方法在哪没用，和知道方法在哪有用，同样是工程结论。

### Q20 如果官方评测机时间限制很严/很松，方案怎么变？
**口径**：严 → 窗口收窄（±60s，296 次仿真，P95 风险 6.45 换更高吞吐）；
松 → 直接全程扫描拿满分。我们的架构是一个可调的"速度-精度旋钮"，
两个极端之间的任一点都能配置——这是混合架构相对单一方案的本质优势。

---

## 7. C 端工程

### Q21 模型怎么部署到 C 的？有精度损失吗？
**口径**：`export_mlp_c.py` 把权重展平为 `mlp_weights.h`（28,801 个 float32 常量），
C 端手写前向（ping-pong 缓冲、ReLU、sigmoid），零运行时依赖。

精度：训练本身就在 float32 GPU 上做，导出不引入额外量化；
特征构造与 `train_mlp.py build_features` 逐维对齐（接入时逐项核对过顺序）。
单次前向 ~3.8µs，相对毫秒级物理仿真可忽略。

### Q22 线上一次完整决策的开销？
**口径**：mock 路线（2789s、6 段）上：v1 引擎 121 次仿真；
v2 引擎 980 次后缀仿真 / 720 万积分步，运行 ~1.7s（含打印）。
内存：权重 28,801×4B ≈ 113KB 常量，加两个 128 宽缓冲，嵌入友好。

### Q23 怎么知道 C 端真的省了仿真？
**口径**：代码内埋了 `g_sim_calls / g_sim_steps` 计数器，
`-DNO_MLP` 编译开关做同代码 A/B——不是估的，是数出来的。
（mock 上 MLP 预测 796.9s vs 物理最优 ~883s，偏 ~86s，±120s 窗口罩住。）

---

## 8. 高压追问（考察诚实与深度）

### Q24 "8.6×"是 Python 口径，线上 C 端实际快了多少？
**口径**：诚实回答——v1 口径 4.3%（更优），v2 口径 ~1%。
加速比取决于引擎，我们报告每个口径的准确数字，不拿最好看的说事。
真正的承诺是：**仿真量同比下降一个数量级的潜力，且任何情况下精度不劣于基线**。

### Q25 训练集 T_init=0℃/SOC=65% 固定，线上遇到别的初态怎么办？
**口径**：特征里含 T_init/SOC，模型见过插值能力可外推；更根本的是，
初态偏离会先反映为可行窗移动——物理可行界定位（不依赖 MLP）先剪掉大部分错误，
MLP 只需在剩余区间内不 grossly 出错。极端失准走 2.24% 那条回退路径。

### Q26 为什么不直接训练一个"得分最大化"的网络？
**口径**：试过方向（得分选点/窄窗加权都在训练实验里），
但得分的 min-max 归一化依赖整个可行集的范围（R_C/R_E）——
网络无法在只见单 case 输入的情况下知道全局范围，这是结构性信息缺失，
不是容量问题。该信息只能靠物理搜索在线获得——又一次印证"精度归物理"。

### Q27 只能留一个组件，留哪个？
**口径**：**物理搜索**。MLP 是优化器不是保底者——去掉 MLP 退化为纯物理（满分但慢），
去掉物理搜索退化为直出（快但 32% 不可行）。我们的全部设计都围绕
"MLP 失效时系统仍正确"展开。这个答案不是示弱，是架构原则。

### Q28 别的队纯物理也能满分，你们的意义是什么？
**口径**：三点。(1) 满分方案在算力受限/长路线/批量决策场景下不可扩展，
我们给出 8.6× 削减且可调；(2) 我们交付的不只是方案，是**可复现的证据链**
（四方案对比、两代引擎 A/B、误差预算分解）；(3) 方法论可迁移——
任何"仿真贵、粗略先验便宜"的决策问题（空调预冷、行程规划）都适用同一框架。

---

## 9. 主动加分点（找机会说）

1. **两次碰壁都是我们自己量化出来的**：直出失败不是"感觉不好"，
   是 67.8% 直通率 + 17.15 分差的评估；PPO 失败是三指标对照——
   我们用数据推翻自己的方向，这比用数据支持自己更严谨。
2. **同代码 A/B 开关**（`-DNO_MLP`）：所有性能声明都能一键复现，
   排除编译环境干扰。
3. **误差预算思维**：把"模型不准"从缺陷表述转成设计输入——
   先测分辨率需求（0.2s）与供给（50s），再决定分工。
4. **兜底哲学**：整个系统没有任何单点：MLP 错了有窗口回退，窗口错了有全程扫描，
   全程扫描错了那是物理模型本身的问题（超出方案范围）。

## 10. 现场演示预案

| 评委要求 | 演示 | 预期输出 |
|---|---|---|
| 跑提交版 | `make run`（student_solution_fast.c） | 约束全过 + Performance 行：MLP-guided yes, 980 calls |
| 对比无 MLP | 编译 `-DNO_MLP` 版运行 | 逐位一致输出, 990 calls |
| 看 MLP 预测 | 运行输出 t_pred | 796.9s（物理最优 ~883s，偏 86s） |
| 看评估数据 | `python ml/plot_narrative.py` | 四张图重新生成 |
| 看文档 | 《方案演进》《MLP流程与加速作用》 | 叙事 + 数据双文档 |

---

### 附：关键数字速查卡

| 数字 | 值 | 出处 |
|---|---|---|
| MLP 前向 | 3.8 µs / 28,801 参数 | C 端实测 |
| MLP 直出 | 直通 67.8%，全体得分差 17.15 | eval_direct.py |
| PPO 直出 | 直通 94.95%，可行子集差 9.89，MAE 127.2s | eval_direct.py |
| 混合方案 | 298 次仿真（8.6×），得分差 1.74，回退 2.24% | eval_window_search.py |
| MLP+安全层 | 通过率 100%，得分差 3.47 | eval_offline.py |
| 误差预算 | 52.2s = 3.5 + 48.7 | 双可行子集实测 |
| v1 mock | 121 次仿真 = 全扫 4.3% | C 计数器 |
| v2 A/B | 980 vs 990，输出逐位一致 | C 计数器 |
| Oracle | 1,067 vs 25,479 评估（17×），Δt=0 | bench 对拍 |
| 物理参数 | P_heat 6kW / C 100Ah / U 350V / SOC 80% 目标 | 赛题 3.5 节 |
| 数据划分 | 可行 case：训练 41,832 / 验证 5,275 / 测试 5,313（10 万条中 52.4% 可行） | oracle_raw.csv |
| MLP 训练 | AdamW lr=1e-3，batch 1024，余弦退火，200 轮上限 + patience 20 早停 | train_mlp.py |
| PPO 配置 | clip 0.2，300 迭代 × batch 512，lr 3e-5，σ 初始 0.05，MLP 热启动 | ppo_finetune.py |
| (4) 方案对比 | (2) 得分 41.755（差 0.02） vs (4) 39.231（差 2.54）；(4) 仿真量 5.7× | cmp_v2_v4_mock.py |
| 扰动测试 | 固定点通过率 (2) 1/9 vs (4) 3/9 → 鲁棒性真答案是重规划 | cmp_robust_mock.py |
